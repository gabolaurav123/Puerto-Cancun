{
  "name": "puerto-cancun-center",
  "version": "1.0.0",
  "description": "Puerto Cancun Center real estate site with seller and admin panels backed by PostgreSQL.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check whatsapp-service.js && node --check seo-pages.js && node --check pdf-property-sheet.js && node --check location-catalog.js && node --check platform-utils.js && node --check db/migrations.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "baileys": "^7.0.0-rc13",
    "bcryptjs": "^3.0.3",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.18.1",
    "pdfkit": "^0.19.1",
    "pg": "^8.13.1",
    "pino": "^10.3.1",
    "qrcode": "^1.5.4",
    "sharp": "^0.35.3"
  },
  "engines": {
    "node": ">=20"
  }
}
